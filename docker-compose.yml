version: '3.8'
services:
  video-finder:
    build: .
    image: video-finder-algorithm:latest
    container_name: video-finder
    ports:
      - "5001:5001"
    volumes:
      - ./:/app
    environment:
      - YOUTUBE_API_KEY=${YOUTUBE_API_KEY}
    restart: unless-stopped
